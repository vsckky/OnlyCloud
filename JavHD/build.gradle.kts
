// use an integer for version numbers
version = 1


cloudstream {
    language = "id"
    // All of these properties are optional, you can safely remove them

    // description = "Lorem Ipsum"
     authors = listOf("vsc")

    /**
     * Status int as the following:
     * 0: Down
     * 1: Ok
     * 2: Slow
     * 3: Beta only
     * */
    status = 1 // will be 3 if unspecified
    tvTypes = listOf(
        "NSFW",
    )

    iconUrl = "https://www.google.com/url?sa=t&source=web&rct=j&opi=89978449&url=https://javhd.today/&ved=2ahUKEwiRqLLtzr-TAxUOaCoJHf1fC54QFnoECBAQAQ&usg=AOvVaw3yXdE9sJT1JU0mawTvY3-W"
}